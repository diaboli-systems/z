func : ( param_one ) testfunc {
    new param &any : param_one

    println : "hello {{param}}"
}

call : ( "Hello", "World!" ) testfunc

new param &str : "tesasdfsdfsdf"

if : param = "test"
    println : 1
elif : param = "test2"
    println : 2
else
    println : 3
end